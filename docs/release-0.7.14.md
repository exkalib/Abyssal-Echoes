# 0.7.14 发布说明

构建号：1788773077。APK：versionCode 20、versionName 0.7.14；外壳协议保持 11。

包含 0.7.13 的新套装、机械背翼、动态特效、裤靴贴合、武器手势，以及营地 / 设置修正；游戏数据与存档格式不变。

本版补充旧安卓更新器兼容：旧 shell 11 对 ZIP 和解压后资源都限制 160 MiB。0.7.13 的完整资源包超过该限制，因此改用包含所有新增资源的兼容包；未变化的老素材沿用 APK 内置文件，APK 本身仍完整打包。额外带上早期 shell 11 尚未内置的 12 张人物、职业、菌圃和怪物图，支持跳过中间版本直接更新。

发布脚本增加上传前 ZIP / 解压总容量校验与回归测试，禁止再次发布超过旧版上限的资源包。正式渠道为 GitHub Release `game-v0.7.14`，替代 0.7.13 作为最新版本；59 测试网页同步本版。

旧版兼容包包含 412 个文件，解压后 103,898,757 字节；未携带的 207 张运行时图在现存 shell 11 发布标签内均存在且内容未变。完整 APK 构建和签名验证通过，SHA-256：`92a16684213079bc65d89e7d68555a1689195257c94b684ae7b823cd3b8090a3`。容量门禁的真实 ZIP 小样覆盖正常、压缩超限、解压超限、缺失及无效归档。
