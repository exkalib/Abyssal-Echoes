{
  "name": "abyssal-echoes",
  "private": true,
  "scripts": {
    "test": "node prototype/game.test.js && node --test netlify/tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.12"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.2",
    "@netlify/database": "2.0.0"
  }
}
